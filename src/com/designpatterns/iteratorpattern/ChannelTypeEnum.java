package com.designpatterns.iteratorpattern;

// An enumeration or enum for short serves the purpose of representing a group of named constants

public enum  ChannelTypeEnum {  //enum

    ENGLISH, SPANISH, FRENCH, ALL;
}